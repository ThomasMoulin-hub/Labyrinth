package BackEnd;

public interface DistanceInterface {

    int minDistance(VertexInterface vertex); // Donne la distance entre vertex et le sommet de départ
    int distance(VertexInterface vertex1, VertexInterface vertex2); // Donne la distance entre deux sommets

}